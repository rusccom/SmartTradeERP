{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go mod tidy && go build -o app ./cmd/api && go build -o app-migrate ./cmd/migrate"
  },
  "deploy": {
    "preDeployCommand": "MIGRATIONS_DIR=/app/migrations /app/app-migrate",
    "startCommand": "/app/app"
  }
}
